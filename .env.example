# Example environment file for MidRange OpsHub Flutter app
# ------------------------------------------------------------------------------

# Base URL for your backend proxy or Jira API (if accessible)
JIRA_BASE_URL=https://support.nov.com

# Jira project keys (help desk + DevOps boards)
JIRA_SUPPORT_PROJECT_KEY=SUPPORT
JIRA_DEVOPS_PROJECT_KEYS=CSSDO,CAPSE

# Note: This app assumes Okta-protected APIs are accessed through a secure backend proxy.
# DO NOT place sensitive auth tokens directly in this file.

# Rename to ".env" and place in the project root before running:
# flutter pub get && flutter run